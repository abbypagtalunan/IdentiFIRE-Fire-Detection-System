
  # Fire Detection UI

  This is a code bundle for Fire Detection UI. The original project is available at https://www.figma.com/design/OxLrsaeGYwJAegGkknac6I/Fire-Detection-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  